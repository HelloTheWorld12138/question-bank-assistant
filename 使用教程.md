# 高中物理题库助手使用教程

这套工具用于把高中物理题目整理成本地题库，并支持搜索组卷、导出 Word。

老师日常只需要使用入口文件，不要打开 `static/index.html`：

- macOS：双击 `打开高中物理题库.command`。
- Windows：双击 `start.bat`。
- `打开Obsidian库.bat`：打开 Obsidian 题库库，用来查看和整理题目文件。

## 一、启动题库网页

macOS 直接双击：

```text
打开高中物理题库.command
```

它会在后台启动题库并自动打开正确的网址。程序没有变化时，重复双击只会再次打开页面；
如果程序刚更新过，启动器会先关闭旧后台并自动加载新版本。不使用时可以双击
`关闭高中物理题库.command`。

Windows 打开项目文件夹：

```text
F:\文档\WhiteCaps
```

2. 双击：

```text
start.bat
```

3. 等待黑色窗口出现，并保持它不要关闭。

4. 浏览器会自动打开。如果没有自动打开，手动访问：

```text
http://127.0.0.1:8000
```

注意：黑色窗口是本地服务。窗口关闭后，网页就不能继续使用。

## 二、导入一道题

网页顶部只有三个主板块：

- “录入题库”：批量导入资料，或手动录入一道题；
- “查看题库”：查找、编辑和维护已有题目；
- “导出题目”：整理已选题目并生成 Word。

进入“录入题库”后，选择“批量导入”可以一次处理 DOCX、PDF、扫描件或照片；只录入
一道题时选择“录入单题”。

批量导入步骤：

1. 选择题目文件，可再选择一份教师版答案与解析文件。
2. 点击“开始整理”。
3. 逐题核对题目、“答案与解析”和分类。系统会尝试读取“答案、解析/详解、难度、
   来源、知识点”等栏目；没有明确栏目时留给老师手动填写，不会把原题重复填入解析。
4. 在“答案与解析”下方展开“查看完整排版效果”，可连续预览题目、答案和解析中的
   文字、公式与图片。
5. 必要时旋转图片、切换去阴影、恢复原图，或在图片上直接框选裁剪范围、拖动四角
   进行透视校正。处理图片后页面会停留在当前题。
6. 勾选“内容已核对”。
7. 点击“确认入库”。

批量分析不会提前占用题号，也不会把 OCR 结果直接写进正式题库。未安装本地 OCR
时，数字 PDF 和 Word 仍可导入；扫描件与照片会保留原图供人工录入。

选择“录入题库 → 录入单题”，填写基本信息：

- 板块：例如“力学”
- 题目特点：例如“创新题”
- 题型：选择题、填空题、实验题、计算题或其他
- 难度：1 到 5
- 年份：例如 `2026`
- 来源：例如“教师自编”“高三月考”

然后填写：

- 题目正文
- 答案
- 解析

如果题目有图片，可以在“上传题目、答案、解析附件或图片”处上传图片。

点击：

```text
检查题目
```

## 三、从 Word 读取题目

如果题目已经在 Word 文件里，可以使用“从 Word 读取内容”。

操作步骤：

1. 选择一个 `.docx` 文件。
2. 选择填入位置：
   - 填入题目正文
   - 填入答案
   - 填入解析
3. 点击“读取内容”。

读取后，系统会把 Word 内容填进对应输入框。文字、可编辑公式和图片会一并读取。
旧版 MathType 公式或 WMF/EMF 题图会自动转成可显示的 PNG 并保留在正文原位置；
斜体、粗体和上下标会在预览中按原格式显示。教师只需核对内容是否完整。

Word 里的图片会自动提取，并在“检查后入库”的图片预览中显示。可以把图片放入题目、
答案或解析，也可以移除不需要的图片。确认入库后，系统会按题号重命名，例如：

```text
LXCX0001_01.png
LXCX0001_02.png
```

如果个别 Word 图片格式无法识别，也可以在附件区手动补充上传图片。

## 四、审核并入库

填写完成后，到“检查后入库”区域检查内容和显示效果。

可以补充：

- 知识点：每行一个
- 附加类型：每行一个，可不填
- 备注：可不填

确认无误后点击：

```text
确认并入库
```

系统会自动生成题号，例如：

```text
LXCX0001
```

题号规则由系统自动管理，不需要老师手动填写。

入库后，每道题会保存为一个 Markdown 文件，位置在：

```text
vault\题目
```

图片和附件会保存到：

```text
vault\assets
```

## 五、搜索题库并组卷

选择“查看题库 → 题库列表”。如果不想逐项填写筛选条件，可以先在“按要求找题”中输入：

```text
找 5 道近三年力学创新题，中等难度，覆盖牛顿第二定律，约 45 分钟，带解析
```

点击“开始找题”后，页面会显示候选题、题目预览、推荐理由和预计用时。默认使用本地
规则，不需要联网。点击单题“加入组卷”或“将推荐题加入组卷”后，仍可在下方调整
顺序和展示题号。

“使用已配置模型优化候选排序”是可选项。云模型只会在老师确认后收到找题要求、
候选题元数据和短预览，不会收到答案、完整解析、图片或整个题库。

展开“按条件筛选”后，可以按下面条件搜索：

- 板块
- 主类型
- 难度
- 年份
- 来源
- 知识点
- 题型
- 全文关键词
- 排序方式

点击：

```text
搜索题库
```

搜索结果会显示：

- 题号
- 板块
- 类型
- 难度
- 来源
- 题目预览
- 编辑、复制和删除按钮

勾选需要加入试卷的题目。

点击“编辑”可以修改题目、答案、解析、知识点、标签、难度、年份和来源。题号、板块和主类型是永久编号的一部分，普通编辑不能修改。

点击“复制”会保留题目内容并生成新的永久题号；题目图片也会复制并绑定到新题号。

勾选多道题后，可以点击“批量修改”，统一增加或移除类型、知识点，或者统一设置题型。

点击“删除”只会把题目和对应图片移动到回收站，不会立即永久删除。

如果选错了，可以点击：

```text
清空选择
```

## 六、导出 Word

在“导出 Word”区域选择导出范围：

- 只导出题目
- 题目 + 答案
- 题目 + 答案 + 解析

还可以设置：

- A4 单栏、A4 双栏或正式考试卷模板；
- 考试时间和满分；
- 班级以及姓名、学号、得分栏；
- 是否在卷面显示题库题号；
- 答案和解析是否另起一页；
- 是否按题型分组；
- 是否分别生成题目卷、答案卷和解析卷。

“试卷题目顺序与展示题号”列表中可以上移、下移或移除题目，也可以把卷面题号改成
`A1`、`1-1` 等形式。题库内部永久题号不会因此改变。

然后点击：

```text
生成试卷
```

先填写试卷标题。系统会为每次导出生成独立名称，例如：

```text
exports\20260726_高二力学周测_题目.md
exports\20260726_高二力学周测_题目.docx
```

旧试卷不会被覆盖。如果只看到 Markdown 下载链接，说明 Pandoc 没有正确安装或没有被检测到。

当前项目已经内置 Pandoc，位置是：

```text
tools\pandoc\pandoc.exe
```

正常情况下可以直接导出 Word。

如果发行包内置了 OfficeCLI，导出结果还会显示：

- DOCX 结构校验结果；
- 版式问题数量；
- “打开 Word 排版预览”链接。

OfficeCLI 不可用时，Pandoc 生成的 Word 仍会保留，不影响教师继续使用。

## 七、题库维护和恢复

“题库维护”区域提供：

- 检查图片：报告缺失图片和没有题目引用的孤立图片；
- 修复编号索引：扫描正式题目并重建 `index.json`；
- 恢复默认模板：覆盖被误改或损坏的三套 Word 模板；
- 立即备份：把题目、图片和编号索引保存为 ZIP；
- 回收站恢复：恢复误删题目及其绑定图片；
- 备份恢复：用历史 ZIP 替换当前题库，恢复前会自动保存当前数据。

程序启动时每天最多创建一次自动备份。执行恢复前仍建议先确认备份文件时间。

## 八、用 Obsidian 查看题库

如果想像笔记库一样浏览题目，可以双击：

```text
打开Obsidian库.bat
```

或者在 Obsidian 中手动打开本地库：

```text
F:\文档\WhiteCaps\vault
```

Obsidian 库里已经安装这些插件：

- Dataview
- Templater
- QuickAdd
- Pandoc Plugin
- Advanced URI

建议 Obsidian 主要用于查看、搜索和轻量整理题目。

正式入库、编号、组卷和导出，仍建议使用网页工具完成。

## 九、可选 AI 辅助分类

网页底部“AI 辅助设置”支持：

- 阿里云百炼 / 通义千问；
- DeepSeek；
- 自定义 OpenAI 兼容服务（包括 LM Studio）；
- 本机 Ollama。

国内联网环境优先选择阿里云百炼或 DeepSeek；如果电脑已经安装本地模型，可选
Ollama。填入 API Key 后点击“保存设置”和“测试连接”。

生成题目草稿后，可以点击“生成分类建议”。云端调用前会提示确认，而且只发送
当前题目正文，不发送答案、解析或整个题库。AI 给出的板块、类型、知识点、题型和
难度只是建议，仍需老师检查并点击“确认入库”。

打开“只使用本地功能”后，程序不会连接云模型。

## 十、文件夹说明

```text
WhiteCaps
├─ app                  网页工具后端
├─ static               网页界面
├─ vault                Obsidian 题库库
│  ├─ 题目              每道题一个 Markdown 文件
│  ├─ assets            图片和附件
│  ├─ 模板              Obsidian 模板
│  ├─ backups           自动和手动备份
│  ├─ .trash            软件回收站
│  └─ index.json        题号编号索引
├─ exports              导出的试卷文件
├─ tools
│  └─ pandoc            项目内置 Pandoc
├─ start.bat            启动网页工具
└─ 打开Obsidian库.bat   打开 Obsidian 库
```

## 十一、常见问题

### 1. 打不开网页

先确认是否已经双击 `start.bat`。

黑色窗口必须保持打开。

如果浏览器没有自动打开，手动访问：

```text
http://127.0.0.1:8000
```

### 2. Word 导出失败

确认项目里是否有：

```text
tools\pandoc\pandoc.exe
```

如果有，重新启动 `start.bat` 后再试。

### 3. 题号能不能手动改

不建议手动改。

题号由程序根据“板块 + 类型”自动生成，并记录在：

```text
vault\index.json
```

手动修改可能导致重复编号。

### 4. 可以直接在 Obsidian 里新建题目吗

可以查看和轻量编辑，但不建议直接新建正式题目。

正式题目建议通过网页工具入库，这样题号和图片命名不会乱。

### 5. Word 里的图片没有显示怎么办

系统会尽量从 `.docx` 中提取图片，并在确认入库时按题号重命名。

如果没有显示，通常是 Word 内图片格式比较特殊。可以这样处理：

1. 先用 Word 转 Markdown 提取文字。
2. 把图片另存为 PNG 或 JPG。
3. 在附件区手动上传图片。
4. 确认入库时由系统按题号重命名图片。
