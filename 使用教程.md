# 高中物理题库助手使用教程

这套工具用于把高中物理题目整理成本地题库，并支持搜索组卷、导出 Word。

老师日常只需要使用两个入口：

- `start.bat`：打开题库网页工具。
- `打开Obsidian库.bat`：打开 Obsidian 题库库，用来查看和整理题目文件。

## 一、启动题库网页

1. 打开项目文件夹：

```text
F:\文档\WhiteCaps
```

2. 双击：

```text
start.bat
```

3. 等待黑色窗口出现，并保持它不要关闭。

4. 浏览器会自动打开。如果没有自动打开，手动访问：

```text
http://127.0.0.1:8000
```

注意：黑色窗口是本地服务。窗口关闭后，网页就不能继续使用。

## 二、导入一道题

网页分为四个区域：

- 导入题目
- 审核入库
- 搜索组卷
- 导出 Word

先在“导入题目”区域填写基本信息：

- 板块：例如“力学”
- 主类型：例如“创新题”
- 难度：1 到 5
- 年份：例如 `2026`
- 来源：例如“教师自编”“高三月考”

然后填写：

- 题目正文
- 答案
- 解析

如果题目有图片，可以在“上传题目、答案、解析附件或图片”处上传图片。

点击：

```text
生成题目草稿
```

## 三、从 Word 转成 Markdown 草稿

如果题目已经在 Word 文件里，可以使用“Word 转 Markdown 草稿”。

操作步骤：

1. 选择一个 `.docx` 文件。
2. 选择填入位置：
   - 填入题目正文
   - 填入答案
   - 填入解析
3. 点击“转换并填入”。

转换后，系统会把 Word 内容填进对应文本框。

Word 里的图片会尽量自动提取，并在“审核入库”的图片预览中显示。确认入库后，系统会按题号重命名，例如：

```text
LXCX0001_01.png
LXCX0001_02.png
```

如果个别 Word 图片格式无法识别，也可以在附件区手动补充上传图片。

## 四、审核并入库

生成草稿后，到“审核入库”区域检查内容。

可以补充：

- 知识点：每行一个
- 附加类型：每行一个，可不填
- 备注：可不填

确认无误后点击：

```text
确认入库
```

系统会自动生成题号，例如：

```text
LXCX0001
```

题号规则由系统自动管理，不需要老师手动填写。

入库后，每道题会保存为一个 Markdown 文件，位置在：

```text
vault\题目
```

图片和附件会保存到：

```text
vault\assets
```

## 五、搜索题库并组卷

在“搜索组卷”区域，可以按下面条件搜索：

- 板块
- 主类型
- 难度
- 年份
- 来源
- 知识点

点击：

```text
搜索题库
```

搜索结果会显示：

- 题号
- 板块
- 类型
- 难度
- 来源
- 题目预览

勾选需要加入试卷的题目。

如果选错了，可以点击：

```text
清空选择
```

## 六、导出 Word

在“导出 Word”区域选择导出范围：

- 只导出题目
- 题目 + 答案
- 题目 + 答案 + 解析

然后点击：

```text
生成试卷
```

系统会生成：

```text
exports\exam.md
exports\exam.docx
```

如果只看到 `exam.md`，说明 Pandoc 没有正确安装或没有被检测到。

当前项目已经内置 Pandoc，位置是：

```text
tools\pandoc\pandoc.exe
```

正常情况下可以直接导出 Word。

## 七、用 Obsidian 查看题库

如果想像笔记库一样浏览题目，可以双击：

```text
打开Obsidian库.bat
```

或者在 Obsidian 中手动打开本地库：

```text
F:\文档\WhiteCaps\vault
```

Obsidian 库里已经安装这些插件：

- Dataview
- Templater
- QuickAdd
- Pandoc Plugin
- Advanced URI

建议 Obsidian 主要用于查看、搜索和轻量整理题目。

正式入库、编号、组卷和导出，仍建议使用网页工具完成。

## 八、文件夹说明

```text
WhiteCaps
├─ app                  网页工具后端
├─ static               网页界面
├─ vault                Obsidian 题库库
│  ├─ 题目              每道题一个 Markdown 文件
│  ├─ assets            图片和附件
│  ├─ 模板              Obsidian 模板
│  └─ index.json        题号编号索引
├─ exports              导出的试卷文件
├─ tools
│  └─ pandoc            项目内置 Pandoc
├─ start.bat            启动网页工具
└─ 打开Obsidian库.bat   打开 Obsidian 库
```

## 九、常见问题

### 1. 打不开网页

先确认是否已经双击 `start.bat`。

黑色窗口必须保持打开。

如果浏览器没有自动打开，手动访问：

```text
http://127.0.0.1:8000
```

### 2. Word 导出失败

确认项目里是否有：

```text
tools\pandoc\pandoc.exe
```

如果有，重新启动 `start.bat` 后再试。

### 3. 题号能不能手动改

不建议手动改。

题号由程序根据“板块 + 类型”自动生成，并记录在：

```text
vault\index.json
```

手动修改可能导致重复编号。

### 4. 可以直接在 Obsidian 里新建题目吗

可以查看和轻量编辑，但不建议直接新建正式题目。

正式题目建议通过网页工具入库，这样题号和图片命名不会乱。

### 5. Word 里的图片没有显示怎么办

系统会尽量从 `.docx` 中提取图片，并在确认入库时按题号重命名。

如果没有显示，通常是 Word 内图片格式比较特殊。可以这样处理：

1. 先用 Word 转 Markdown 提取文字。
2. 把图片另存为 PNG 或 JPG。
3. 在附件区手动上传图片。
4. 确认入库时由系统按题号重命名图片。
